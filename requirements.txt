fastapi==0.138.0
uvicorn==0.49.0

SQLAlchemy==2.0.51
# alembic==1.18.4
asyncpg==0.30.0
psycopg2-binary==2.9.12

pydantic==2.13.4
email-validator==2.3.0
pydantic-settings==2.14.2
python-dotenv==1.2.2

passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.5.0

minio==7.2.20
python-multipart==0.0.32

python-docx==1.2.0
# paddlepaddle 3.3.x has a CPU/oneDNN PIR regression that crashes PPStructureV3
# layout detection (github.com/PaddlePaddle/Paddle/issues/77340) — pin 3.2.2.
paddlepaddle==3.2.2
paddleocr==3.7.0
# paddlex's "ocr" extra hard-requires opencv-contrib-python by exact package
# name (swapping to opencv-python-headless fails its dependency check even
# though cv2 itself works fine) — needs libGL.so.1 at runtime as a result;
# install libgl1 + libglib2.0-0 on any minimal/server image (e.g. Render).
opencv-contrib-python==4.10.0.84
pymupdf==1.28.0
boto3

pinecone==9.1.0

langchain-text-splitters==1.1.2
langgraph==1.2.9
openai==2.45.0
arq==0.28.0
tiktoken==0.13.0

alembic==1.18.5
